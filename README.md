# kalymma

A screen overlay library for Windows and Linux. It gives an application a fixed-size ARGB pixel
surface anchored to a screen edge, plus pointer and keyboard input. The caller owns the pixels and
kalymma owns the window.
